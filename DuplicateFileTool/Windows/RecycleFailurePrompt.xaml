<Window x:Class="DuplicateFileTool.Windows.RecycleFailurePrompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:DuplicateFileTool.Properties"
        xmlns:local="clr-namespace:DuplicateFileTool"
        Title="{x:Static resx:Resources.RecyclePrompt_Title}"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height"
        Width="480"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:RecycleFailurePromptViewModel, IsDesignTimeCreatable=False}">
    <StackPanel Margin="12">
        <TextBlock Text="{x:Static resx:Resources.RecyclePrompt_Header}" TextWrapping="Wrap" />
        <TextBlock Text="{Binding FilePath, Mode=OneWay}" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,6,0,0" />
        <TextBlock Text="{Binding Reason, Mode=OneWay}" TextWrapping="Wrap" Margin="0,6,0,0" />
        <CheckBox Content="{x:Static resx:Resources.RecyclePrompt_Apply_To_All}" IsChecked="{Binding ApplyToAll}" Margin="0,12,0,0" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{x:Static resx:Resources.RecyclePrompt_Delete_Permanently}" MinHeight="28" Padding="10,3" Margin="0,0,6,0" Click="OnDeletePermanentlyClick" />
            <Button Content="{x:Static resx:Resources.RecyclePrompt_Ignore}" IsDefault="True" MinHeight="28" Padding="10,3" Margin="0,0,6,0" Click="OnIgnoreClick" />
            <Button Content="{x:Static resx:Resources.RecyclePrompt_Cancel}" IsCancel="True" MinHeight="28" Padding="10,3" Click="OnCancelClick" />
        </StackPanel>
    </StackPanel>
</Window>
